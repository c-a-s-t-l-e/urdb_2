# Notes:

Look at `_targets.R` for the pipeline.

Look at `\R` for the code that `_targets.R` uses

----

Also, create `\data` and `\outputs` folders.

And, download the URDB json file from here and place it in the `\data` folder:
https://apps.openei.org/USURDB/download/usurdb.json.gz

---

Lastly, when you push changes to the repo, note that .gitignore ignores both the `\data` and `\outputs` folders as well.
